# Start on a new machine

```bash
chezmoi init --apply https://github.com/stoeps13/chezmoi-dotfiles
```

## Install

- pre-commit (python or pacman)
- betterleaks (AUR)

After cloning the repo, initialize the pre-commit script:

```bash
pre-commit install
```
